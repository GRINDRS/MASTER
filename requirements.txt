# Core server & web framework
Flask==3.0.2

# MQTT communication
paho-mqtt==1.6.1

# OpenAI client
openai>=1.3.0

# Environment variable helper
python-dotenv>=1.0.0

# Speech processing
SpeechRecognition==3.10.0
gTTS==2.5.4
pyaudio==0.2.14

# Computer-vision & imaging
opencv-python==4.10.0.82
Pillow>=10.0.1
numpy>=1.23

# Raspberry Pi GPIO access (safe to ignore on non-Pi systems)
rpi-lgpio

# --------------------------------------------------
# System-level dependencies (install via OS package manager, NOT via pip)
# --------------------------------------------------
# These are listed here for convenience; `pip` will ignore lines starting with
# a hash symbol. Make sure they are installed on the host machine or in your
# Docker image.
#   • ffmpeg            – for audio playback (`gTTS` output)
#   • mosquitto         – MQTT broker that the services connect to
#   • mosquitto-clients – command-line MQTT utilities (optional, helpful for debugging)
#   • portaudio19-dev   – required headers/libs so `pyaudio` can compile (Debian/Ubuntu)
#   • libasound2-dev    – ALSA sound dev package (also for `pyaudio` on some distros)
# Example (Ubuntu):
#   sudo apt update && sudo apt install -y ffmpeg mosquitto mosquitto-clients \
#        portaudio19-dev libasound2-dev
